{
  "type": "object",
  "required": ["copy_id", "total_score", "total_possible", "questions", "expert_instructions_used"],
  "properties": {
    "copy_id": {"type": "string"},
    "total_score": {"type": "integer", "minimum": 0},
    "total_possible": {"type": "integer", "minimum": 1},
    "expert_instructions_used": {"type": "boolean"},
    "questions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["rubric_item_id", "score", "confidence", "comment", "observed_answer", "requires_review"],
        "properties": {
          "rubric_item_id": {"type": "string"},
          "score": {"type": "integer", "enum": [0, 1]},
          "confidence": {"type": "number", "minimum": 0.0, "maximum": 1.0},
          "comment": {"type": "string"},
          "observed_answer": {"type": "string"},
          "requires_review": {"type": "boolean"}
        }
      }
    }
  }
}
