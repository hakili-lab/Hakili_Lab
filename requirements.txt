# Hakili Lab — Dépendances Python (versions épinglées pour reproductibilité)
# Mettre à jour avec : pip-compile requirements.in

# --- Core ---
pydantic>=2.9.0
pydantic-settings>=2.5.0
jsonschema==4.23.0
python-dotenv==1.0.1
pyyaml>=6.0.2
python-docx>=1.1.2    # Extraction énoncé DOCX (TestRegistry)

# --- Traitement image & PDF ---
pillow>=10.4.0,<12.0.0
opencv-python-headless==4.10.0.84
pymupdf>=1.24.9        # wheel pré-compilé dispo 1.27+ pour Python 3.13 Windows

# --- Génération PDF ---
reportlab==4.2.2
jinja2==3.1.4
markupsafe>=3.0.0     # utilisé directement dans pdf_report_html.py (pas seulement transitif via jinja2)
xhtml2pdf>=0.2.14

# --- Interface ---
streamlit>=1.36.0
pandas>=2.2.0
openpyxl>=3.1.0        # Lecture .xlsx (import élèves en masse)

# --- Fournisseurs IA ---
anthropic>=0.40.0
google-genai>=1.0.0    # SDK unifié — gemini_client.py utilise `from google import genai`
openai>=1.30.0        # DeepSeek (API compatible OpenAI) + GPT-5 (fallback)
mistralai>=1.0.0,<2.0.0   # Mistral Small 3.1 — v2.x a changé l'API

# --- Réseau & Robustesse ---
httpx>=0.28.1
tenacity==8.5.0

# --- Base de données (portail de consultation Neon Postgres) ---
sqlalchemy>=2.0.23
alembic>=1.13.1
psycopg2-binary>=2.9.9

# --- Google Sheets (source de vérité élèves/profs, compte de service) ---
gspread>=6.1.0,<7.0.0
google-auth>=2.34.0

# --- Qualité code (développement uniquement — ne pas inclure en prod) ---
# pytest==8.3.2
# ruff==0.5.7
# mypy==1.11.1
